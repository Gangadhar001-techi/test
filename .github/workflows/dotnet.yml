trigger:
- main

variables:
- group: my-variable-group

jobs:
- job: ReadVariables
  pool:
    vmImage: ubuntu-latest

  steps:
  - script: |
      echo "The value of myVariable is: $(myVariable)"
      echo "The value of anotherVar is: $(anotherVar)"
    displayName: 'Read group variables'
